{
    "name": "{{name}}",
    "version": "{{version}}",
    "src": {
        {{! if submodules array with each, else string}}
    },
    "test": {
        "functional": "{{functionalTestDirectory}}",
        "integration": "{{integrationTestDirectory}}",
        "unit": "{{unitTestDirectory}}"
    }
}