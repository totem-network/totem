mutation addImages($images: [ImageInput]) {
    addImages(images: $images) {
        result
        image {
            files {
                thumbnail
            }
            metaData {
                name
            }
        }
    }
}