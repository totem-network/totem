query digitalAsset($contract: String) {
    digitalAsset(contract: $contract) {
        description
        feeFast
        feeSafeLow
        id
        image
        name
        asset {
            name
        }
    }
}