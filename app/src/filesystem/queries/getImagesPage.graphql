query imagesPage($first: Int, $after: String) {
    images(first: $first, after: $after) {
        files {
            lowResolutionPlaceholder
        },
        metaData {
            height,
            width
        }
    }
}