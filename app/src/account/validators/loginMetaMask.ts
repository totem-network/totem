import { ILoginMetaMaskData } from './../components/form/LoginMetaMask';

const validate = (values: ILoginMetaMaskData) => {
    const error = {};
    //
    return error;
};

export default validate;
