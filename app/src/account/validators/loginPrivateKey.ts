import { ILoginPrivateKeyData } from './../components/form/LoginPrivateKey';

const validate = (values: ILoginPrivateKeyData) => {
    const error = {};
    //
    return error;
};

export default validate;
