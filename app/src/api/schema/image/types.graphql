type Image {
    file: String
    height: Int
    width: Int
}

type AddImagesResponse {
    images: [Image]
    result: Boolean
}

type Query {
    images: [Image]
}

type Mutation {
    addImages(images: [Upload]): AddImagesResponse
}