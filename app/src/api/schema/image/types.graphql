type Image {
    file: String
    height: Int
    width: Int
}

type AddImagesResponse {
    images: [Image]
    result: Boolean
}

input ImageInput {
    name: String
    dataUrl: String
}

type Query {
    images: [Image]
}

type Mutation {
    addImages(images: [ImageInput]): AddImagesResponse
}